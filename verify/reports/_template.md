<!--
  This file is to /verify-article what content/_reference/article-template.md
  is to /new-article: the authoritative shape of the output. Copy it to
  verify/reports/<slug>-verify.md and fill it in. Guidance lives in comments
  only — comments are stripped when the report is written.

  Rules:
  - No verdicts. Not "correct", not "confirmed", not "矛盾", not ✅/❌.
    Put the claim and the evidence side by side; Kelly judges.
  - Layer 1 code is lifted verbatim from the article. Never retyped.
    Where a block cannot run as published, subdivide by selecting whole
    article lines — never by rewriting them.
  - Layer 4 snippets are the exception — before-snippets are newly written from
    symptom+cause, and after-snippets are frequently assembled or new too.
    Label all of them: 抜粋 / 継ぎ合わせ / 新規作成.
  - Re-verifying a slug appends a new dated section at the bottom.
    Never overwrite an earlier run, and never grade one.

  Formatting — measured values must survive a reformat:
  - Never put a pipe character in a table cell, not even inside a code span.
    A Markdown formatter re-reads it as a column separator and collapses the
    row. This has already destroyed one report's main result table.
    A measured value containing pipes goes in a fenced block or a list below
    the table, with the table cell pointing to it.
  - Prefer a fenced block over a table whenever the values are raw output
    rather than short labels. A table is for comparison at a glance; it is
    not a container for captured stdout.

  Report body is written in Japanese.
-->

# 検証レポート: <slug>

- 対象ファイル: `content/knowledge-base/<category>/<slug>.md`
- 検証日: YYYY-MM-DD
- カテゴリ: <category> → 層1の方法: <jsdom + react-dom/client + act() / plain Node / 対象外(理由)>

<!-- 環境の内訳。複数バージョン/構成を使った場合はどれがどれかを書く。
     verify/package.json に pin されていないパッケージを使った場合は、
     どこから読んだのか、どの項目で使ったのか、
     verify/ 単体では再現しないことを明記する。 -->

<!-- バージョン差分の主張がこの記事に無い場合は、その判断をここに書く。
     代わりにモード差分(sloppy/strict)や実行文脈差分(classic script/ES module)を
     測ったなら、それも書く。 -->

<!-- 再実行の場合はここに:
     - 前回からの記事側の変更(コミットハッシュと、それが何を変えたか)
     - 前回からのskill側の変更
     - その結果、今回新しく測る項目はどれか
     前回セクションの所見に触れる場合は「今回の照合ではこうだった」と
     今回の測定として書く。前回が間違っていた/解消した、とは書かない。 -->

---

## Step 0: 棚卸し

<!-- 実行する前に、記事から拾った候補を全部ここに並べる。
     予測が明示されていない(コメントも隣接文も無い)コード片は
     「判定不能」とラベルする。推測で埋めない。

     「概念レベルの主張」と「Sourcesのエントリ」は別バケツとして扱う。
     どの主張がどのURLの「近く」にあるかでは対応づけない —
     層3ではこの2つを独立した集合として全件照合する。 -->

**検証可能なコード片(N件)**

1. <行番号>: <コードが何をするか> → <記事が主張する出力と、それがどこに書かれているか>
2. ...

**frontmatterのフィールド内にあった検証可能な主張(N件)**

<!-- gotcha の fix / cause / errorMessage、interviewQuestion の答えなどに、
     コード片を伴わずに書かれている検証可能な主張。
     「この書き方では効かない」「この形なら解決する」等。
     fenced block が無いので見落としやすい。層1か層4に持っていく。
     記事本文に対応するコード片が一切無いものは、その旨を明記する。 -->

- <行番号>(`<フィールド名>`): 「<引用>」

**差分の主張(バージョン/モード/実行文脈、N件)**

- <行番号>: 「<引用>」 → 必要な構成: <バージョンN / sloppy と strict / classic script と ES module>

**概念レベルの主張(Sourcesと照合すべきもの、N件)**

<!-- 初出で太字にされた用語、gotchaのcauseに書かれた仕組み、
     「なぜそうなるか」を述べている箇所など。記事中の位置と
     Sourcesの並び順は無関係 — ここでは主張だけを独立して拾う。 -->

- C1(<行番号>): 「<記事からの引用、または主張の要約>」

**`## Sources`のエントリ(N件。うちURLなし N件)**

<!-- URLの無いエントリも1件として数える。fetch対象が無いことを明記し、
     層3の「リンク単体の所見」に送る。URLだけを数えると報告から消える。 -->

1. <URL>(<行番号>)
2. <行番号>「<テキスト>」— **リンクが張られていない。fetch対象なし**

**gotchas(N件)**

1. `<gotcha-slug>`
2. ...

**判定不能とラベルした箇所(N件)**

- <行番号>: <該当箇所と、なぜ判定できないか>

---

## Step 1: 実行(層1)

<!-- 1件につき: 記事からの引用 → 実行したスクリプト → 実測 → 環境。判定は書かない。
     ラッパー(state宣言、APIスタブ等)を書いた場合は、その全文を併記する。
     スニペットを観測可能にするために足した行(記事が呼んでいない呼び出し、
     書くだけで読み返していない値の読み出し等)は、1つずつ名指しする。 -->

### 1. <主張の見出し>(<行番号>)

- 主張(<行番号>): 「<記事からの引用>」
- 実行スクリプト: `verify/scripts/<slug>/<name>.mjs` — <何をするか1行で。コミットしない>
- 追加した行: <無ければ「無し」。あれば1つずつ>
- 実測:

```
<実際の出力>
```

- 環境: <ランタイム / バージョン>

### <公開されている形のままでは走らないブロックがある場合>

<!-- SyntaxError を含むブロックはパース時に落ち、他の行について何も示さない。
     まずブロック全体をそのまま走らせた結果を出し、その上で
     各主張を観測できる最小断片に分ける。断片は記事の行を「選択」したもので、
     書き換えた行があってはならない。どの行が断片なのかを書く。 -->

- 実行スクリプト: `verify/scripts/<slug>/<name>.mjs`
- ブロックが1プログラムとして走らない理由: <どの行が何を投げるか>
- 実測:

```
whole block as published <行範囲>   <結果>

<行範囲>  <断片の内容>              <結果>
<行範囲>  <断片の内容>              <結果>
```

### <否定形の主張がある場合>

<!-- 「この書き方では効かない」「値は変わらない」等。
     差が出ないという結果は、測定系が壊れていても同じ形で出る。
     必ず同じスクリプト内に対照(差が出るはずのケース)を置き、
     その行も併記する。

     対照が期待通りに動いていない場合、記事との比較に入る前に
     測定系を作り直す。対照が壊れている測定は、記事と一致していても
     矛盾していても読めない。作り直した場合は Known pitfalls 再チェックに書く。 -->

- 主張(<行番号>): 「<引用>」
- 実行スクリプト: `verify/scripts/<slug>/<name>.mjs` — <記事からの抜粋か新規作成かを明記>
- 本題と対照:
  - **本題(負)**: <何を示せば主張が支持されるのか>
  - **対照(正)**: <何を示せば測定系が機能していると言えるのか>
- 本題と対照の全文:

```js
<両方の全文>
```

- 実測:

```
[本題]  <出力>
[対照]  <出力>
```

- 対照の位置づけ: <対照がこの値を返していることが、本題の結果を読める形にしている理由を1行で>
- 環境: <ランタイム / バージョン>

### <差分の主張がある場合(バージョン/モード/実行文脈)>

<!-- 主張が名指ししている構成を「全部」走らせる。1つで代表させない。
     バージョン差分なら current と legacy の両方。片方だけでは実証にならない。
     イベントハンドラ内だけを叩くコードは、どのバージョンでも同じ結果になり
     何も証明しない。

     結果は表にしない — レンダー記録のような生の出力にはパイプが入りやすく、
     表に置くと再フォーマットで壊れる。構成ごとの見出し+fencedブロックにする。 -->

- 主張(<行番号>): 「<引用>」
- 主張が名指ししている構成: <列挙し、そのうちどれを測ったかを書く。全部測る>
- 実行スクリプト: `verify/scripts/<slug>/<name>.mjs` — <記事からの抜粋か新規作成かを明記>

**<構成A>**

```
<出力>
```

**<構成B>**

```
<出力>
```

**Known pitfalls再チェック**: <発生した場合、最初の方法・出た結果・なぜ測定系を疑ったか・作り直した方法・2回目の結果。両方の値を残す。発生しなければ「該当なし」と、避けた罠を1行ずつ>

---

## Step 2: バージョン主張 vs 一次資料(層2)

<!-- コードで実証できない主張(なぜ変わったか、等)が対象。
     一次資料の該当箇所を短く引用し、記事の主張の真下に置く。

     「変わっていない」という主張を、リリースノートや deprecated 一覧に
     記載が無いことで支えている場合は、それが消極的証拠であることを明示する。
     そのページが何を列挙しているかを書き、主張の対象がその中に無いことを書き、
     「記載が無いことの提示であって、それ以上のものではない」と言い切る。
     層1で同じ対象を実測できるなら、その実測を併記する。

     fetch が目次・ナビゲーション・表紙しか返さなかった場合は、そう書く。
     節の見出しが版に存在することは、その節の規範本文についての証拠ではない。 -->

### <行番号> — <主張の要約>

**主張**: 「<記事からの引用>」

**一次資料**(<URL>):

> <該当箇所の短い引用>

<!-- 取得範囲に限界があった場合はここに:
     何が取得できたか / 規範本文が読めていないこと /
     したがって何が確認できていないか -->

<!-- 消極的証拠の場合はここに: そのページが列挙しているもの / 対象が含まれないこと /
     これは記載の不在にすぎない、という限定 / 対応する層1の実測への参照 -->

---

## Step 3: Sources監査(層3)

<!-- Sourcesはフラットな参考文献リストであって、1URL=1主張の脚注ではない。
     位置が近いという理由だけで対応づけない。

     手順:
     1. 全URLを先にfetchする(下の表)。URLの無いエントリもこの表に載せる。
     2. Step 0で挙げた概念レベルの主張ごとに、fetch済みの全sourceを対象に
        支持する記述がないか探す。
     3. 主張ごとに「全面カバー / 一部カバー / 該当なし」のいずれかを1つ選ぶ。
        - 全面カバー: どれかのsourceが主張を述べている、または主張を含意する
          一般的な原則を述べている。逐語一致は不要。
        - 一部カバー: sourceは主張の一部を支持するが、記事はそれを超えたことを
          述べている。記事が正しいかどうかは論点ではない。引用が主張の全体を
          運びきっていない、という事実を可視化するための分類。
        - 該当なし: 全sourceを確認して、どれにも見当たらない。
        「この言い方自体はどのsourceにも無いが」と書き始めたら、それは
        一部カバー。全面カバーに畳み込まない。
        ただし単なる言い換え(記事の`const`とsourceの「呼び出しごとに新しく
        作られ再代入されない変数」など)は全面カバー。どちらなのかを1行で書く。
     4. 該当なしはさらに2つに分ける:
        - 一次資料あり: Sourcesには無いが、層2でfetchした一次資料や
          既存sourceの兄弟ページがカバーしている。URLを名指しする。
          Sourcesに1行足せば済むケース。
        - 一次資料なし: どこにも見つからなかった。別のKB記事に委ねている、
          層1の実測に依拠している等の事情があればそれも書く。
     5. source同士が食い違っている場合は、それ自体が所見。どちらがどう言って
        いるかを両方引用する。記事に合う側だけを引かない。
     6. fetch失敗・リンク切れ・URLの無いエントリは、対応する主張の有無と
        関係なく「リンク単体の所見」に書く。 -->

**fetch結果**

| リンク | fetch結果                              |
| ------ | -------------------------------------- |
| <URL>  | 成功 / 失敗(理由) / 目次のみ / URLなし |

<!-- 以降 S1 / S2 / ... の略号を使う場合はここで定義する -->

### 概念レベルの主張ごとの照合(N件)

**C<n> — <主張の見出し>(<行番号>)**

- 主張: 「<記事からの引用>」
- 確認したsource: 全<N>件(fetch済みのSources全体)
- 分類: **全面カバー / 一部カバー / 該当なし(一次資料あり) / 該当なし(一次資料なし)**
- 該当した記述:
  - <S番号>: 「<引用>」 — <逐語一致か、一般的な記述が具体ケースを包含しているか、1行で>
  <!-- 複数のsourceが分担してカバーしている場合は全部列挙する -->
- <source同士が食い違う場合> どのsourceがどちらの言い方を採っているか:
  <!-- 両方引用する。記事に合う側だけを引かない -->
- <一部カバーの場合> sourceが述べているのはどこまでで、記事がそこから何を足しているか:
- <該当なし(一次資料あり)の場合> カバーしている一次資料のURL:

<!-- 主張ごとにこの見出しを繰り返す -->

### 一部カバーとした主張の一覧(N件)

<!-- 上の照合から一部カバーのものだけを行番号つきで再掲する。
     全件を通読しなくてもここだけで拾えるようにするための欄。
     Kellyが記事を直すか判断する対象は主にここ。無ければ「なし」。 -->

- **<行番号>(C<n>)**: <主張の要約> — <sourceが届いていない部分>

### 該当なしの一覧(N件)

<!-- 一次資料あり/なしを分けて書く。
     「一次資料あり」はSourcesに1行足せば済むので、URLをそのまま出す。 -->

**一次資料あり(Sourcesへの追加で解決しうる、N件)**

- **<行番号>(C<n>)**: <主張の要約> — カバーしている一次資料: <URL>

**一次資料なし(N件)**

- **<行番号>(C<n>)**: <主張の要約> — <別のKB記事に委ねている / 層1の実測に依拠している / 見つからなかった>

### リンク単体の所見(N件)

<!-- 特定の主張の有無とは別に、リンク自体が死んでいる/URLが無い/
     目次しか取れなかった等の所見はここに書く。
     取得側の限界なのか記事側の問題なのかを区別する。無ければ「なし」。 -->

- <所見> — <記事側の問題か、この実行の取得側の限界か>

---

## Step 4: gotcha fix検証(層4)

<!-- beforeコードが記事からの抜粋か、symptom+causeから新規に書いたものかを
     必ず明記する。新規に書いた場合は全文を見せる — Kellyは結果だけでなく
     「再現として妥当か」も見る必要がある。

     afterコードも同じ扱い。出所は3種類で表記する:
       抜粋       — 記事の行をそのまま(どの行か)
       継ぎ合わせ — 記事の行の並べ替え、1トークンの置換、記事の別スライスとの結合
                    (どの行を、どう変えたか)
       新規作成   — 記事に対応するコード片が無い
     単一の verbatim なスライスでない限り全文を見せる。
     これまでに見つかった記事の誤りはコードブロックではなく fix の中にあった。

     fix が複数の選択肢を挙げている場合、「全部」を個別に適用して
     それぞれ報告する。3つの箇条書きは3回の after 実行になる。
     「何もしない」という選択肢も1つとして扱い、before のコードで
     そのキーワードが何を止めているのかを観測する。
     適用できない選択肢があれば、黙って落とさずその旨を書く。

     errorMessage フィールドがある場合、実行が出したメッセージと
     フィールドの文言を突き合わせて両方見せる。 -->

### <gotcha-slug>

- symptom: 「<frontmatterからの引用>」
- errorMessage: 「<あれば引用>」
- fix: 「<frontmatterからの引用>」
- fixの選択肢: **N個**((a) <内容>、(b) <内容>)
- beforeコードの出所: **<抜粋(行番号)/ 継ぎ合わせ(何をどう)/ 新規作成>**

```js
<単一の verbatim スライスでない場合はここに全文>
```

- 実測(symptom再現): <実際に起きたこと。errorMessageがある場合は文言の一致も>

**fixの選択肢(a)(<箇条書きの内容>)を適用した版**

- 出所: **<抜粋(行番号)/ 継ぎ合わせ(どの行を、どう変えたか)/ 新規作成>**

```js
<単一の verbatim スライスでない場合はここに全文>
```

- 実測: <実際に起きたこと>

**fixの選択肢(b)(<箇条書きの内容>)を適用した版**

<!-- 選択肢の数だけ繰り返す。適用できないものがあれば理由を書く -->

---

## まとめ(数値のみ、判定なし)

- 棚卸し: コード片**件、frontmatter内の主張**件、差分の主張**件、概念レベルの主張**件、Sourcesエントリ**件(うちURLなし**件)、gotcha**件、判定不能**件
- 層1: **件実行(対象外**件)、Step 0との突き合わせ(層4で扱った**件 / 1スクリプトにまとめた**件)、公開形のままでは走らなかったブロック**件、複数構成で実行**件(構成: **)、否定形の主張**件(うち対照を置いた**件)、Known pitfalls再チェック**回、`verify/`外のパッケージ使用**件、スクリプト**件
- 層2: 一次資料**件fetch、目次のみ**件、消極的証拠(記載が無いことを根拠)**件、要フラグ**件
- 層3: Sourcesエントリ**件中**件fetch、概念レベルの主張**件を全source照合、全面カバー**件 / 一部カバー**件 / 該当なし**件(一次資料あり**件、一次資料なし**件)、source同士の食い違い**件、リンク単体の所見**件
- 層4: gotcha**件中**件検証、fixの選択肢**件中**件を個別適用、beforeの出所(抜粋**/継ぎ合わせ**/新規**)、afterの出所(抜粋**/継ぎ合わせ**/新規**)
