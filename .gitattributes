# Article Markdown is the source of truth for the `article` content type and is
# read line by line by scripts/article-push.mjs. Pin it to LF so a stray CRLF
# checkout cannot show up as a whole-file diff.
* text=auto eol=lf
